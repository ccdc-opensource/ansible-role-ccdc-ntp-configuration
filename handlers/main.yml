---
- name: Restart ntp service
  ansible.builtin.service:
    name: "{{ ntp_daemon }}"
    state: restarted
  become: true

- name: Restart chrony service
  ansible.builtin.service:
    name: "{{ chrony_daemon }}"
    state: restarted
  become: true
